@model IEnumerable<plantillaMVC.Models.TipoCliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoClienteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoClienteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoClienteId })
        </td>
    </tr>
}

</table>
